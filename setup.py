from setuptools import setup, find_packages

setup(name='yaml_include_loader',
      version='0.1',
      description='YAML loader for inclusion of yaml files in yaml files',
      packages=find_packages())

